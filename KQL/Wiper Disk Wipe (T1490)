DeviceProcessEvents
| where Timestamp >= ago(1h)
| where FileName in ("cipher.exe","sdelete.exe") 
      and (ProcessCommandLine has "/w" or ProcessCommandLine has "/q")
| project Timestamp, DeviceName, InitiatingProcessAccountName, ProcessCommandLine
 