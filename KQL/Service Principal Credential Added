AuditLogs
| where OperationName == "Add service principal credentials"
| project TimeGenerated, AppId=tostring(TargetResources[0].userPrincipalName), AddedBy=tostring(InitiatedBy.user.userPrincipalName)
